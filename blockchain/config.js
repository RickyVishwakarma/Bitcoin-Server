const config = {
  DIFFICULTY_ADJUSTMENT_INTERVAL: 10, // Number of blocks after which difficulty is adjusted
  BLOCK_GENERATION_INTERVAL: 10000, // 10 seconds between blocks
  MINING_REWARD: 50, // Mining reward in units
  INITIAL_DIFFICULTY: 4, // Initial difficulty level
  BLOCK_REWARD: 50, // Reward for mining a block
};

module.exports = config;
